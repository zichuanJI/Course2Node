# Course2Node 核心机制与数据科学原理详解

本文档旨在从技术和算法层面，深入剖析 Course2Node 如何将非结构化的教学数据（PDF 讲义、音频实录）一步步转化为高语义密度的知识图谱。文档分为「数据流与知识抽取机制」和「图谱拓扑与前端可视化算法」两大部分。

---

## 1. 数据流与知识抽取机制 (Data Science Pipeline)

### 1.1 文档切块与预处理 (Chunking)
- **机制**：无论是 PDF 文本还是 Whisper 音频转写，在摄入后都并非整篇输入 LLM，而是通过滑动窗口结合语义断点进行切分。
- **算法细节**：
  - **文本切分**：采用基于长度限制结合标点符号（句号、换行等）的启发式切分，确保每个 `EvidenceChunk`（文本块）既包含完整的上下文，又不会超出语言模型和 Embedding 模型的最大 Token 上限。相邻 Chunk 之间会保留一定的 Overlap 以防知识点截断。
  - **音频切片与对齐**：对于音频输入，基于 `faster-whisper` 的处理不仅是转写，还会严格结合时间戳（Timestamp）和静音停顿进行分块。这使得图中每一个概念或原文块，最终都能精确映射并溯源到具体的视频/音频片段。

### 1.2 向量化 (Embedding Generation)
- **机制**：默认使用开源 SOTA 的稠密检索模型 `BAAI/bge-m3`。
- **算法细节**：将上一步产出的每个 Chunk 映射为 1024 维的特征向量。该向量一方面用于支持系统的 Semantic Search（语义搜索），另一方面也是后续计算跨章节节点相似度的基石。同时，生成的每个 `ConceptNode`（概念节点）的 Embedding 是其对应关联文本块 Embedding 的综合表达。

### 1.3 大模型知识抽取 (LLM Graph Extraction)
- **机制**：核心的数据结构化过程。通过精心设计的 System Prompt 要求模型输出符合严格 Schema 的 JSON。
- **算法细节**：
  1. **实体抽取 (Entity Extraction)**：提取 `Concept`，并丰富其元数据，包括定义 (Definition)、关键要点 (Key Points)、标签 (Tags)、前置知识 (Prerequisites) 和应用场景 (Applications)。
  2. **关系抽取 (Relation Extraction)**：提取 `Relation`，除了识别连接的源与目标，还要求模型输出信心值 (Confidence) 和关系类型 (如 `is_a`, `part_of`, `causes`, `prerequisite_of` 等)。
  3. **批处理与防崩溃**：系统根据字符数和 Token 规模自动进行 Batch 分组多线程并发请求。若 LLM 响应超时或由于幻觉生成了无法解析的内容，系统会自动降级（Fallback）到基于规则与统计的抽取引擎。
  4. **统计抽取降级 (Rule-based Fallback)**：当大模型不可用时，系统退化为提取文本中的高频词（TF-IDF思想），并通过统计词组共现矩阵 (Co-occurrence Matrix) 以及正则表达式关键字（如“导致”、“包含”）来硬编码建立关系网，保证系统的高可用性。

### 1.4 图谱度量与核心计算 (Graph Metrics & Centrality)
在概念与边建立后，后端会使用经典的图论算法对知识点的重要性进行定量评估（具体实现在 `graph_builder.py` 中）：
- **无向加权图构建**：不同类型的边被赋予不同的初始权重，例如明确的 `RELATES_TO`（强逻辑相关）权重大于 `MENTIONS`（仅提及）。
- **中心性评估 (Centrality Calculation)**：
  - **加权度中心性 (Weighted Degree Centrality)**：评估节点与周围节点交互的直接强度，权重越高的关系贡献越大。
  - **中介中心性 (Betweenness Centrality)**：使用 **Brandes 算法** ($O(V \cdot E)$ 复杂度)。计算该节点存在于图中其他所有节点对最短路径上的频率。中介中心性高的节点通常是跨主题的**桥梁/枢纽知识点**。
  - **接近中心性 (Closeness Centrality)**：计算节点到图中其他所有节点的平均最短距离的倒数，衡量其在知识网络中的可达性。
- **综合重要度打分 (Importance Score)**：
  系统采用加权总和来判定概念的最终要死度：`0.45 * 加权度 + 0.25 * 中介度 + 0.20 * 接近度 + 0.10 * 基础度`。该得分决定了节点在总图谱中的留存和前端的视觉层级。

### 1.5 实体消歧与多图谱归并 (Entity Resolution & Merging)
- **机制**：在一门课程的不同讲次中，大模型往往会提取出名称不同但指代相同的概念（如“线性回归”与“Linear Regression”）。系统在生成总图谱前，会进行严格的实体消歧与合并。
- **算法细节**：
  - **合并原则**：系统依赖 `canonical_name`（规范化命名）作为全局唯一主键，将跨章节的相同概念映射到同一节点。
  - **属性融合 (Union & Max)**：在合并时，非破坏性地取各个子节点 `aliases`（别名）、`tags` 和 `key_points` 的并集，并通过提取长度最长的字符串来保留最详尽的 `definition` 和 `summary`，从而有效防止总图谱中的“概念爆炸”（Node Explosion）。

### 1.6 跨章节语义融合 (Cross-chapter Semantic Bridging)
- **机制**：单次抽取是在章节内部进行的，LLM 无法知晓全局。在归并生成**课程总图谱**时（`course_graph_builder.py`），系统会将各章 Top 6 重要度的节点进行融合。
- **算法细节**：合并同名节点后，系统利用事先计算好的 `bge-m3` 向量，对**没有直接连线**的节点对计算余弦相似度 (Cosine Similarity)。若相似度 $\ge 0.55$，系统会自动补全一条 `semantic_similarity` 的跨章节虚拟边。这使得原来彼此孤立的章节岛屿在总图谱中得以有机联结，真正实现"知识织网"。

### 1.7 混合语义检索引擎 (Hybrid Semantic Search)
- **机制**：在左侧搜索栏进行检索时，系统并不单纯依赖向量数据库，而是采用**稠密向量与词法匹配融合（Hybrid Search）**的策略，实现在概念（Concepts）与原文块（Chunks）中的双向精准寻回。
- **算法细节**：
  - **概念检索**：除了计算查询语句向量与节点向量的 Cosine Similarity，还会提取查询的关键分词。如果节点名称包含搜索词，会额外增加 `0.25` 的词法奖励得分（Lexical Bonus）。
  - **原文块检索**：计算向量相似度外，按 Chunk 包含查询分词的频次，增加 `0.1 * 词频` 的得分补偿，从而在模糊意图检索（靠 Embedding）和具体术语检索（靠词频）间取得完美平衡。

### 1.8 生成式知识下游应用 (Generative Applications)
基于提纯后的图谱，系统能够进行高效的二次生成降维（`notes.py` 与 `exam.py`）：
- **结构化笔记降维**：图谱是一个非线性的网络，但人类复习习惯线性阅读。系统根据大模型提取的节点定义和关系群，将 Top 重要度的节点按依赖关系重组，通过 LLM 润色输出为层级分明、排版优美的 Markdown/TeX 讲义，实现“书越读越薄”。
- **自适应试卷生成**：基于节点的重要度（Importance Score），从图谱中抽取核心概念作为“考点”，利用生成模型自动组配出包含选择题、简答题在内的自测题库。

---

## 2. 前端图谱可视化算法 (Frontend Layout Algorithms)

前端通过 ReactFlow 渲染节点，但并没有使用其默认或外部的黑盒布局，而是为了保证展示逻辑与教学逻辑高度一致，在 `layoutUtils.ts` 中手写了三种高级布局引擎。

### 2.1 力导向布局 (Force-Directed Layout)
- **目的**：展现自然生长的知识拓扑，让关联紧密的知识点自动聚拢，毫不相干的知识点被排斥推开。
- **算法细节**：基于 **Fruchterman-Reingold 算法** 进行修改：
  1. **冷启动防飞散优化 (Cold-Start Optimization)**：普通 FR 算法若初始点全随机，很容易在第一步由于巨大的斥力让节点“飞出屏幕”。在此引擎中，初始状态强制所有节点均匀排列在一个极小的同心圆上 (`Math.cos(angle) * k * 0.6`)，这极大地加速了向全局能量最低点的收敛过程。
  2. **排斥力 (Coulomb's Law)**：任意两节点间存在 $O(N^2)$ 的斥力，距离越近斥力越大 ($F = k^2 / d$)，防止节点重叠。
  3. **吸引力 (Hooke's Law)**：存在边的节点之间像弹簧一样相互吸引 ($F = d^2 / k$)。
  4. **引力场 (Gravity)**：添加了指向画布中心的全局引力，防止离群节点飞到视区外。
  5. **模拟退火冷却 (Simulated Annealing)**：随着迭代次数增加，限制节点最大位移的"温度"逐渐降低，使图谱最终稳定在极小能量态。

### 2.2 径向布局 (Radial Layout)
- **目的**：凸显课程的核心重点。将最重要的概念置于绝对的视觉中心，次要概念按圈层向外扩散。
- **算法细节**：
  1. **度数排序**：按节点的 Degree（连接数）从大到小降序排列。
  2. **核心定位**：将度最大的节点锚定在原点 `(0, 0)`。
  3. **同心圆分布**：其余节点被分配到半径递增（如 $200px, 380px, 560px...$）的同心环上。
  4. **弧长碰撞避免**：动态计算当前圆环的周长能容纳的最大节点数（限制最小弧距 $\ge 200px$），放满后溢出的节点自动降级到下一更远的环，确保高密度的图谱依然清晰可读。

### 2.3 聚类布局 (Cluster Layout)
- **目的**：宏观展示课程的几个大主题模块。
- **算法细节**：基于后端的连通图发现（Connected Components）算法找出的社区群组（Topic Clusters），按照颜色将节点分组：
  1. **子群圈定**：计算每个聚类群需要的内环半径 $R_{inner}$（由群内节点数决定），将同一群组的节点均匀排列在这个小圆上。
  2. **大群防碰撞**：将每个子群视作一个带着缓冲区的“巨型节点” (Footprint)。
  3. **宏观轨道**：计算出一个巨大的宏观半径 $R_{outer}$（确保最极端的两个子群也不会发生碰撞），最后将所有群的中心点均匀排列在这个宏观大圆的轨道上。
  最终效果呈现为**"大圆套小圆"**的星系状分布，非常适合观察模块化的章节分布。

### 2.4 宏微观结合的子图下钻机制 (Sub-graph Drill-down)
- **目的**：面对海量的课程总知识点，一次性全盘渲染会导致性能崩溃与视觉污染（Hairball 效应）。
- **算法细节**：系统基于核心概念元数据 (`course_meta`)，默认仅渲染最重要的“骨架节点”。当用户在骨架中点击某个核心概念时，触发广度优先搜索（BFS），在内存中动态抓取拓扑深度 `Depth = 1` 的局部邻居子图进行渲染。这实现了宏观骨架与微观知识群落的平滑“下钻”（Drill-down）交互体验。

---

## 3. 为什么这样设计？

从大模型抽取到前端渲染，Course2Node 的每一步技术选型都服务于"教学认知":
- **为什么使用 Brandes 和 接近中心性？** 在复习时，我们不仅要看一个概念被提了多少次（Degree），更需要知道哪些概念是连接不同章节的桥梁（Betweenness）。掌握这些"桥梁"往往意味着打通了学科的任督二脉。
- **为什么要做 Embedding Similarity 跨章桥接？** 因为教材是线性撰写的。第一章提到的基础概念在第四章应用时，文本上可能没有明确写出关联。向量检索能在潜在高维语义空间发现这种"暗线"，补足显式规则的缺漏。
- **侧边栏 (ConceptDrawer) 为什么自动收起/打开？** 在图形探索时，信息过载是最大敌人。侧边栏联动设计让用户的焦点始终能够集中在图谱拓扑与实体详请的交替上，通过自动响应操作来保持视野的开阔。
